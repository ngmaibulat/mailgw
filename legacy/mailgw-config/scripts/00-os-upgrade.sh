sudo apt update
sudo apt upgrade
sudo reboot


cat /etc/os-release

sudo vim /etc/update-manager/release-upgrades

sudo do-release-upgrade

### Upgrade to devel release
# sudo do-release-upgrade --devel-release 

sudo reboot

sudo apt autoremove

cat /etc/os-release
