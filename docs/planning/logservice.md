# logservice-bun — remaining work

Tracking only what's left; task-level history lives in `logservice-bun/TODO.md`.

1. **Deferred models / endpoints** (do only when needed)
   `Header`, `Log`, `Config`, `RelayGroup`, `Relay`, `User`, `Exception` — no
   active routes use them yet; port on demand.

See also: `planning/testing.md` (all testing work), `planning/plugins.md`
(Haraka plugin hardening), and `docs/apikey.md` (API-key auth setup).
