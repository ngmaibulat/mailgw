extends ../page.pug
block content
    div(id="content" class="p-3")
        h3 Something went wrong
        p.text-muted Error #{statusCode}
        p= message
        a.btn.btn-primary(href="/") Return home
